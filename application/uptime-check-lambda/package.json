{
  "name": "uptime-check-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist/ && tsc && cp .env dist/.env",
    "build:clean": "rm -f lambda-package.zip && rm -rf node_modules && lerna bootstrap && npm run build",
    "build:deploy": "npm run build:clean && cp -LR node_modules dist/node_modules && rm -rf dist/node_modules/@types && cd dist && zip -r ../lambda-package.zip . * && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "@types/node": "^16.7.3",
    "@types/node-fetch": "^2.5.12",
    "@types/request": "^2.48.7",
    "typescript": "^4.4.2"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.27.0",
    "@aws-sdk/util-dynamodb": "^3.27.0",
    "abort-controller": "^3.0.0",
    "dotenv": "^10.0.0",
    "request": "^2.88.2",
    "types": "^1.0.0"
  }
}
